// import { api } from '@shared/api/axios';
// import { useMutation } from '@tanstack/react-query';
// import { syncUserToDb } from '../api/userApi';

// type SyncPayload = {
//   userId: string;
//   fcmToken: string;
// };

// export function useSyncFcmToken() {
//   return useMutation({
//     mutationFn: async ({ userId, fcmToken }: SyncPayload) => syncUserToDb(userId, fcmToken),
//   });
// }
